FROM python:3.11-slim

WORKDIR /app

# System deps some wheels (chromadb, sentence-transformers) may need to build/run
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# MOCK_LLM defaults to mock/offline mode (graded baseline). Override with
# `docker run -e MOCK_LLM=0 ...` only for the optional real-LLM extension.
ENV MOCK_LLM=1
ENV PORT=7860

EXPOSE 7860

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7860"]
